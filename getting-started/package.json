{
  "name": "tfjs-examples-micro",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=8.9.0"
  },
  "scripts": {
    "build": "mkdirp dist && cpx index.html dist/ && cpx index.js dist/",
    "watch": "npm run build && http-server dist -p 1234"
  },
  "devDependencies": {
    "clang-format": "^1.8.0",
    "cpx": "^1.5.0",
    "cross-env": "^10.0.0",
    "http-server": "^14.1.1",
    "mkdirp": "^3.0.1"
  },
  "resolutions": {
    "follow-redirects": "1.14.8"
  }
}
